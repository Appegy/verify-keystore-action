{
  "name": "verify-keystore-action",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "GitHub Action to verify Android Keystore validity, password, and alias.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run typecheck && ncc build src/main.ts -o dist --license licenses.txt",
    "format": "prettier --write '**/*.ts'",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^2.0.0",
    "@actions/io": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}